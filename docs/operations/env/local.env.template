# Local template (default profile)
# Usage:
#   cp docs/operations/env/local.env.template .env.local
#   export $(grep -v '^#' .env.local | xargs)

SPRING_PROFILES_ACTIVE=default

# Local observability defaults
FRAUD_ENGINE_DEFAULT_ACTUATOR_EXPOSURE=health,info,metrics
FRAUD_ENGINE_DEFAULT_SWAGGER_UI_ENABLED=true
FRAUD_ENGINE_DEFAULT_API_DOCS_ENABLED=true

# Optional local threshold overrides (usually keep defaults)
# FRAUD_RULE_HIGH_AMOUNT_REVIEW_THRESHOLD=10000.00
# FRAUD_RULE_HIGH_AMOUNT_BLOCK_THRESHOLD=25000.00
